export default interface Patient {
  name: string;
  lastName: string;
  startDate: Date;
  type: string;
  gender: string;
  telephone: string;
  address: string;
  birthPlace: string;
  birthDate: string;
  postalCode: string;
}
